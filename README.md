## Kommando för att sätta mail vid publicering
 az webapp config appsettings set --resource-group gymtracker-rg --name gymtracker-app --settings ADMIN_EMAIL="din@email.com" ADMIN_PASSWORD="dittLösenord123!"
